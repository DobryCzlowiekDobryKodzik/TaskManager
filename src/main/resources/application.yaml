spring:
  application:
    name: TaskManager
  datasource:
    url: jdbc:mysql://localhost:3306/task-manager-db?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
